# # Default setupでは本ymlは適用されないので、使いたい場合は Advanced setup へ切り替えが必要
# name: CodeQL

# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]
#   schedule:
#     # ｓｓ
#     - cron: "30 18 * * 0"

# permissions:
#   contents: read
#   actions: read
#   security-events: write

# jobs:
#   analyze:
#     name: Analyze (Python)
#     runs-on: ubuntu-latest

#     strategy:
#       fail-fast: false
#       matrix:
#         language: ["python"]

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Initialize CodeQL
#         uses: github/codeql-action/init@v3
#         with:
#           languages: ${{ matrix.language }}

#       - name: Autobuild
#         uses: github/codeql-action/autobuild@v3

#       - name: Perform CodeQL Analysis
#         uses: github/codeql-action/analyze@v3
